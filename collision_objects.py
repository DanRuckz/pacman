import pygame
import pacman
import enemy
import map
import auxiliary

pacmanBox = pacman.Player.rect
#enemyBox = enemy.Enemy.rect
"""fullMap = map.fullMap
def checkCollisionWithMap(objectRect):
   for element in fullMap:
       if auxiliary.hitboxCollision(pacmanBox,element) == True:
            return True

    #for element in backgroundArray:
        #auxiliary.hitboxCollision(objectRect,element)
"""